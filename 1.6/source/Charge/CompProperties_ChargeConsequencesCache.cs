using Verse;

namespace Chargeable_Hediffs_Framework
{
    public class CompProperties_ChargeConsequencesCache : CompProperties
    {
        public CompProperties_ChargeConsequencesCache()
        {
            compClass = typeof(CompChargeConsequencesCache);
        }
    }
}
